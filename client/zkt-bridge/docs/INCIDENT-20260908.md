# Production verification and unresolved historical incident

Permanent SSH access uses alias `farmplast-zkt`, user `tony lomakin`, dedicated
ED25519 fingerprint SHA256:tX34kkzyNqKpFrCltZF/vDmQqMU4Y/Z0K+QOwbsXa9A.
Existing authorized keys and administrator-only ACLs were preserved. No sshd
restart, firewall/Tailscale change, password authentication change or user action
was needed. An existing bridge-specific key provided the bootstrap channel.

The working production service/executable were not modified or restarted.
At capture NSSM PID 22308 and application PID 10616 started about 07:00 EDT.
Exactly one zkt-bridge.exe ran on nj-5591-tl. No local ZKT service/worker was found
on the development computer at inspection time.

## Real UI tests on 2026-09-08 (UTC)

| Operation / command ID | Created | Picked | Finished | Total |
|---|---|---|---|---|
| test / 23a53f44-cb18-4354-99d1-386018880099 | 16:22:02.649414 | 16:22:03.217 | 16:22:05.541 | 2.892s |
| sync_one_employee / dac3e1b5-2475-449c-99ae-d1fee789b6ba | 16:23:03.634311 | 16:23:04.402 | 16:23:07.299 | 3.665s |
| pull_attendance / faa6b11a-8ad5-484d-93e6-d529b9801c47 | 16:23:53.391716 | 16:23:54.168 | 16:24:14.912 | 21.520s |

All returned `done`, `result.ok=true`, and visible UI success without timeout.
The sync used the existing employee from the original incident through the normal
UI. Attendance reported total 8167, inserted 193, skipped 7974. No employees,
attendance records or ZKT users were deleted. Production also independently
completed sync command 71c0451d-56c5-4f2f-8ba3-dd11708e0f37 before these tests.

## Historical failures: do not overstate causality

Command 1cffedfb-2358-4eca-92e8-4a35801abb4c was created 2026-09-07
19:02:23.005948 EDT, picked 19:17:54.237, finished 19:17:54.481, with
`Unknown command: sync_one_employee`. Queue delay was 931.231 seconds; execution
after claim was 244ms. This proved that the handling worker rejected the command;
it did not identify that worker's machine or executable.

Targeted searches of active nj-5591-tl logs did not find that ID or the adjacent
failed ID. Its current binary demonstrably supports sync_one_employee, and its
current 32-bit COM initialization and SDK operations pass. Earlier DB attendance
results reported 0x80040154, but this was not reproduced on the current NJ worker.
Another worker or historical runtime mismatch is plausible, not proven. Absence
from logs alone is not proof of a separate worker. No blind code/config repair
was made for these unproven causes.

Observed fallback polling is 300000ms (five minutes), with Realtime broadcast
wake enabled. It is a latency risk if wake delivery fails, but does not by itself
prove why this specific command waited 15.5 minutes. The UI/build inspected in the
earlier turn differs from the live UI observed today (which offers NJ/PA actions).

Morning attendance logs showed database statement timeouts followed by successful
row-by-row fallback. Current operation works; this remains a performance finding,
not justification for speculative database changes.

Alternate remote directories: C:\ztk-m300-plus-bridge (installer/config/docs only),
C:\FarmplastZktWorker.disabled-20260709-151025 (SDK only), plus historical worker ZIPs
under Downloads. None was run or substituted for the active production executable.

Editable original source, original npm lockfile, worker attribution for historical
DB writes, and a fresh-machine recovery drill remain missing.
